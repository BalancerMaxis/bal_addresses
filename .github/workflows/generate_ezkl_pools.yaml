name: Generate EZKL Pools JSON
permissions:
  contents: read

on:
  schedule:
    - cron: "0 0 * * 3"
  workflow_dispatch:

jobs:
  refresh_ezkl_pools:
    runs-on: ubuntu-latest

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          token: ${{ steps.app-token.outputs.token }}

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Update Files
        id: update
        env:
          DRPC_KEY: ${{ secrets.DRPC_KEY }}
        run: |
          pip3 install -r bal_addresses/requirements.txt
          python3 gen_ezkl_pools.py
          git add -A

      - name: pull-request
        id: create-pr
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ steps.app-token.outputs.token }}
          commit-message: "Scheduled updates to EZKL pools JSON"
          title: "Scheduled updates to EZKL pools JSON"
          branch: "gha-ezklpools"
          reviewers: "gosuto-inzasheru"
          labels: "EZKL Pools"

      - name: Enable auto-merge
        if: steps.create-pr.outputs.pull-request-number
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: gh pr merge --auto --merge ${{ steps.create-pr.outputs.pull-request-number }}